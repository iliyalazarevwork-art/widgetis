node_modules
# dist is committed — pre-built locally via `npm run build:prerender`
# and copied verbatim into nginx by frontend/Dockerfile.
.vite
*.local
.logs
*.log

# Playwright artifacts (videos, traces, error-context.md, .last-run.json)
test-results/
playwright-report/
blob-report/
.playwright/
